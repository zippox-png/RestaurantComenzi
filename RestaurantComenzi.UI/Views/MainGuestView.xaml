<UserControl x:Class="RestaurantComenzi.UI.Views.MainGuestView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Caută:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <TextBox Width="250" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        <ItemsControl Grid.Row="1" ItemsSource="{Binding ProduseAfisate}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="10" CornerRadius="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Rectangle Fill="LightGray" Width="80" Height="80"/>
                            <StackPanel Grid.Column="1" Margin="10,0">
                                <TextBlock Text="{Binding Denumire}" FontWeight="Bold" FontSize="16"/>
                                <TextBlock Text="{Binding CantitatiAfisate}"/>
                                <TextBlock Text="{Binding Alergeni}" FontStyle="Italic" Foreground="Gray"/>
                                <TextBlock Text="Indisponibil" Foreground="Red" Visibility="{Binding Disponibil, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            </StackPanel>
                            <StackPanel Grid.Column="2" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Pret, StringFormat={}{0} lei}" FontSize="18" HorizontalAlignment="Right"/>
                                <Button Content="Adaugă în coș" Command="{Binding DataContext.AddToCartCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding Id}" Margin="0,5,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>